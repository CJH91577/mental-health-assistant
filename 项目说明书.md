# 心理健康助手 —— 项目说明书

> 版本：部署版 v1.0　|　部署日期：2026-09-05　|　部署位置：`C:\Users\admin\心里健康助手全栈项目`

---

## 一、项目简介

「心理健康助手」（前端品牌名"心理健康AI助手"，AI 会话名"宁渡AI助手"）是一个面向大学生群体的心理健康应用，采用前后端分离架构，提供四大核心能力：

| 模块 | 说明 |
|------|------|
| AI 心理咨询 | 基于 DeepSeek-V3 大模型的流式对话（SSE），支持多会话管理与情绪分析展示 |
| 情绪日记 | 情绪评分（1-10）+ 8 种情绪卡片 + 睡眠/压力记录，支持 AI 情绪分析 |
| 知识科普 | 心理健康知识文章分类浏览、阅读量统计、后台发布/下线管理 |
| 数据统计 | 管理端数据看板（情绪趋势、咨询活动、用户活跃度三类图表 + 统计卡） |

**用户角色**：普通用户（userType=1）与管理员（userType=2），分别对应用户端与后台管理端两套界面。

---

## 二、运行环境（本机已就绪）

| 组件 | 版本 | 说明 |
|------|------|------|
| 操作系统 | Windows 11 | |
| JDK | 21.0.12（Oracle） | Spring Boot 3.5.15 兼容，pom 按 Java 17 编译 |
| Maven | 3.9.16 | 项目自带 mvnw 包装器也可用 |
| Node.js | v24.16.0 + npm | 前端构建/开发需要 |
| MySQL | 8.0（服务 MySQL80，端口 3306） | 密码通过环境变量 `MYSQL_PASSWORD` 配置（本机已设置，不写入代码） |
| 编辑器 | VSCode（前端）/ IntelliJ IDEA（后端） | 已安装 |

**端口约定**：后端 `1235`；前端开发模式 `5174`（被占用时自动 +1）；数据库 `3306`。

---

## 三、快速启动（部署后日常使用）

### 方式一：一键脚本（推荐）
1. 双击 `启动后端.bat` → 后端启动（内含已构建好的前端页面）。
2. 浏览器访问 **http://localhost:1235** 即可使用，无需再开前端。

### 方式二：前端开发模式（改前端代码时用）
1. 先启动后端（方式一或 IDEA 里运行 `AiSpingbootApplication`）。
2. 双击 `启动前端开发模式.bat`，按终端提示的地址访问（5174 起）。
3. 开发模式已配置 `/api` 与 `/files` 代理到 `http://localhost:1235`。

### 默认账号（密码均为 `123456`）
| 账号 | 角色 | 登录后进入 |
|------|------|-----------|
| `admin` | 管理员 | 后台管理端（数据看板/文章管理/咨询记录/情绪日志） |
| `test` / `ces` | 普通用户 | 用户端（AI 咨询/情绪日记/知识库） |

也可以在注册页自行注册新账号（注册的账号默认为普通用户）。

---

## 四、AI 接口配置（重要）

AI 心理咨询默认调用**硅基流动（SiliconFlow）**的 DeepSeek-V3 接口，也支持切换为 **DeepSeek 官方**接口。未配置 Key 时对话会返回友好提示"AI 服务暂时不可用"。

> 注意：两家平台的 Key **互不通用**——硅基流动的 Key 只能配合 `api.siliconflow.cn` 使用，DeepSeek 官方的 Key 只能配合 `api.deepseek.com` 使用。

**配置方法（环境变量 + 重启后端即可，无需重新打包）**：

| 您的 Key 来自 | 需要设置的环境变量 |
|--------------|------------------|
| 硅基流动（siliconflow.cn 控制台） | `SILICONFLOW_API_KEY` = 您的 sk-xxx |
| DeepSeek 官方（platform.deepseek.com） | `AI_API_KEY` = 您的 sk-xxx；`AI_BASE_URL` = `https://api.deepseek.com`；`AI_MODEL` = `deepseek-chat` |

设置环境变量：`Win + R` 输入 `sysdm.cpl` → 高级 → 环境变量 → 用户变量 → 新建；或管理员 CMD 执行 `setx 变量名 "值"`。设置后**重启后端**（重新双击 `启动后端.bat`）。

**可选**：换模型只需设置 `AI_MODEL`（硅基流动填如 `deepseek-ai/DeepSeek-V3.2`；DeepSeek 官方填 `deepseek-chat` 或 `deepseek-reasoner`）。

**验证**：AI 咨询页发送消息应有流式回复；提交情绪日记后，后台「情绪日志 → 详情」应出现 AI 情绪分析。

**排查**：若仍提示不可用，依次检查——Key 是否 `sk-` 开头且无多余空格；新开 CMD 执行 `echo %AI_API_KEY%` / `echo %SILICONFLOW_API_KEY%` 看是否显示；设置后是否重启了后端；平台账户是否有余额/免费额度。
> 特别注意：重启后端前先确认没有旧的后端进程占用 1235 端口（如任务管理器里的 java.exe），否则新进程会因端口被占用而启动失败，页面仍连到不带 Key 的旧进程上。后端控制台现在会打印 AI 调用失败的真实原因（ERROR 日志），可按日志排查。

---

## 五、系统架构

```
浏览器 (http://localhost:1235)
   │  前端: Vue3 + Vite + Element Plus + ECharts + wangEditor（打包进后端 static/）
   ▼
Spring Boot 3.5.15 后端 (端口 1235)
   ├─ /api/**            业务接口（JWT 认证，Spring Security 无状态）
   ├─ /files/**          上传文件静态服务（本地 uploads 目录）
   └─ / 及 SPA 路由      前端页面（Vue Router history 模式回退）
   ▼
MySQL 8.0 数据库 mental_health_assistant（9 张表）
   ▼（AI 对话/情绪分析）
硅基流动 API（DeepSeek-V3，https://api.siliconflow.cn）
```

- **认证**：JWT（HMAC256，24 小时有效），请求头名 `token`；登录接口返回 `token + userInfo + roleType`。
- **流式对话**：前端 `fetchEventSource` POST `/api/psychological-chat/stream`，SSE 事件 `message`/`done` 逐段渲染。
- **统一响应**：`{ code, msg, data }`，`code="200"` 为成功；错误提示由前端 Axios 拦截器统一弹窗。

### 后端包结构（org.example.aispingboot）
```
controller  接口层（用户/咨询/日记/知识库/文件/数据分析/SPA回退）
service     业务层（含 convert 转换）
mapper      MyBatis-Plus BaseMapper + 统计 SQL
entity      实体（User/ConsultationSession/ConsultationMessage/EmotionDiary/
            KnowledgeArticle/KnowledgeCategory/SysFileInfo）
DTO         command / query / response
config      Security/JWT/ChatClient/Web(CORS+静态)/MyBatisPlus 分页
util        JWT 工具与过滤器、响应工具
common      Result 统一返回、ResultCode、全局异常处理、PageResult
AiService   提示词、心理支持服务（ChatClient + ChatMemory）、结构化输出
```

### 数据库（9 张表）
`user`、`consultation_session`、`consultation_message`、`emotion_diary`、`ai_analysis_task`、`knowledge_category`、`knowledge_article`、`user_favorite`、`sys_file_info`。
建库脚本与演示数据：`资料\mental_health_assistant.sql`（已导入）。

### 主要接口清单
| 模块 | 接口 |
|------|------|
| 用户 | POST `/api/user/login`、`/api/user/add`、`/api/user/logout`、GET `/api/user/current` |
| 心理咨询 | POST `/api/psychological-chat/session/start`、`/stream`(SSE)；GET `/sessions`、`/sessions/{id}/messages`、`/session/{id}/emotion`；DELETE `/sessions/{id}` |
| 情绪日记 | POST `/api/emotion-diary`；GET `/api/emotion-diary/admin/page`；DELETE `/api/emotion-diary/admin/{id}` |
| 知识库 | GET `/api/knowledge/category/tree`、`/article/page`、`/article/{id}`；POST/PUT/DELETE `/article`、`/article/{id}`；PUT `/article/{id}/status` |
| 文件 | POST `/api/file/upload`（返回 `filePath`，经 `/files/**` 访问） |
| 统计 | GET `/api/data-analytics/overview` |

---

## 六、本次部署所做的修复与优化

### 后端（ai-spingboot）
1. **修复 spring-ai 依赖不可用**：`1.0.0-SNAPSHOT`（快照版不在中央仓库）→ 改为 GA 稳定版 `1.0.0`，本地可离线构建。
2. **补全了缺失的全部后端模块**（原源码只有用户+咨询两模块，前端调用的接口大部分不存在）：
   - 用户登出、会话列表/详情/删除/情绪分析；
   - 情绪日记：新增（同一天仅一条）、后台分页查询（含用户昵称）、后台删除；
   - 知识库：分类列表、文章分页/详情（阅读数+1）、创建/更新/上下线/删除；
   - 文件上传（本地存储 + `sys_file_info` 落库 + `/files/**` 静态服务 + 仅允许图片类型）；
   - 数据看板 `/data-analytics/overview`（概览统计卡、情绪趋势、咨询统计、用户活跃度，缺失日期补 0）。
3. **配置修正**：端口统一为 `1235`；数据库密码、JWT 密钥、AI Key 均改为环境变量注入（本机已配置，代码中不留真实密码）；JWT 头统一为 `token`；`serverTimezone` 改为 Asia/Shanghai。
4. **安全与健壮性**：新增 CORS 配置；`/files/**`、OPTIONS、知识库只读接口放行；上传文件白名单（jpg/jpeg/png/gif/webp）防恶意文件；AI 服务异常时返回友好提示事件而非静默断流；清理了调试 `System.out.println`。
5. **新增 SPA 路由回退**：前端打包进后端 `static/`，刷新/直连深层路由（如 `/back/dashboard`）不再 404。
6. **分页参数兼容**：后端同时接受 `pageNum/pageSize`、`currentPage/size`、`current/size` 三套参数名（前端各页面不统一）。

### 前端（ai-vue）
1. **崩溃级 Bug 修复**：`emotionDiary.vue`、`frontendKnowledge.vue`、`articleDetail.vue` 从 element-plus 导入不存在的 `dayjs`（页面白屏）→ 改为原生格式化。
2. **请求层重写**（`utils/request.js`）：非 200 业务码统一弹错并 reject；401/token 失效清理缓存跳登录；超时 5s→20s；网络异常友好提示。
3. **路由守卫加固**：`JSON.parse(userInfo)` 增加 try/catch，缓存损坏时按未登录处理。
4. **XSS 修复**：文章详情接入 DOMPurify 消毒；Markdown 渲染器链接仅允许 http/https/mailto；咨询页用户消息先转义再渲染。
5. **功能补全**：咨询页 Enter 发送（Shift+Enter 换行）、情绪花园空数据兜底（不再崩溃）、新建会话清空对话区；首页两个按钮接通跳转。
6. **体验修正**：后台侧边栏高亮跟随路由；情绪日志/咨询记录列标签与实际数据对齐；AI 情绪分析为空时显示空态提示；数据看板图表数据兜底。
7. **环境配置本地化**：代理与文件地址由远程服务器 `159.75.169.224:1235` 改为本地 `localhost:1235`；页面标题改为"心理健康AI助手"。

### 数据
- 导入 `mental_health_assistant.sql`（9 张表 + 演示数据）。
- 演示账号密码统一重置为 `123456`（原库密码未知）。

---

## 七、日常操作指引

| 操作 | 方法 |
|------|------|
| 启动服务 | 双击 `启动后端.bat`，访问 http://localhost:1235 |
| 停止服务 | 后端控制台 Ctrl+C（或任务管理器结束 java 进程） |
| 重新打包后端 | 在 `ai-spingboot` 目录执行 `mvn -DskipTests clean package` |
| 重新构建前端 | 在 `ai-vue` 目录执行 `npm run build`，再复制 `dist\*` 到 `ai-spingboot\src\main\resources\static\` 并重新打包 |
| 改数据库密码 | 修改 `application.yml` 的 `spring.datasource.password` |
| 开启 AI 对话 | 见"四、AI 接口配置" |

---

## 八、注意事项与已知限制

1. **AI 对话依赖外部 Key**：未配置硅基流动 Key 时对话与日记 AI 分析不可用，其余功能全部正常。
2. **AI 对话记忆为内存实现**（`MessageWindowChatMemory`，保留 30 条），后端重启后历史上下文丢失（历史消息仍保存在数据库，可回看）。
3. 本机 5173/5174/5175 端口存在旧的前端副本进程（`C:\Users\admin\vs-code\ai-vue` 启动的 vite），与本次部署无关，可自行关闭。
4. 上传的图片保存在后端目录 `uploads\` 下，删除文章时不会连带删除磁盘文件（属正常行为）。
5. JWT 密钥、数据库密码当前为明文配置，仅适合本机/教学环境，如需公网部署请改用环境变量与强密钥。
6. 生产建议：将 `spring.ai` 配置抽离为环境变量、给 `uploads` 目录做定期备份。
7. **控制台乱码**：启动脚本已按中文 Windows 的默认编码 GBK 保存（控制台、Java 日志输出同为 GBK，显示一致）。请勿用编辑器把 `.bat` 改存为 UTF-8，否则中文提示会乱码。

---

## 九、目录速查

```
心里健康助手全栈项目\
├── 启动后端.bat                     # 一键启动（含前端页面）
├── 启动前端开发模式.bat             # 前端开发模式
├── 项目说明书.md                    # 本文件
├── ai-vue\                          # Vue3 前端源码
├── ai-spingboot\                    # Spring Boot 后端源码（static/ 内为打包后的前端）
└── 资料\                            # SQL 脚本、技术文档、环境安装包
```
